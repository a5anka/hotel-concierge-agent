openai>=1.50.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pytest>=8.3.0
httpx>=0.27.0
langgraph>=0.2.0,<1.0
langchain-openai>=0.2.0,<1.0
langchain-core>=0.3.0,<1.0
# Pin wrapt<2 because Agent Manager auto-instruments via amp-instrumentation,
# which transitively pulls opentelemetry-instrumentation-langchain==0.53.3.
# That instrumentor calls wrap_function_wrapper(module=..., name=...) which
# wrapt 2.x rejected (kwarg removed). Without this pin the LangChain
# instrumentor crashes at startup ("Error initializing LangChain instrumentor:
# wrap_function_wrapper() got an unexpected keyword argument 'module'") and
# zero LangGraph spans reach the trace panel — the entire Act 2 demo moment
# silently breaks. Remove this pin only after upgrading both the WSO2
# amp-instrumentation chain and traceloop-sdk past their wrapt-2 fix.
wrapt<2
